# Runtime dependencies for neutronimaging

astropy  # FITS reading in the contract tests; explicit rather than transitive via NeuNorm
docopt
h5py  # used by NeuNorm 1.x loader but not declared as its dependency
ipympl
matplotlib
numpy
NeuNorm<2  # 2.0 is a breaking rewrite; detector_correction.py uses the 1.x API
pandas
timepix-geometry-correction>=0.2.0.dev7  # explicit dev spec: only pre-releases published so far
tqdm
