# Runtime dependencies for neutronimaging

astropy  # FITS frame loading (was transitive via NeuNorm 1.x)
docopt
ipympl
matplotlib
numpy
pandas
tifffile  # float32 TIFF export (was via NeuNorm 1.x/PIL)
tqdm
