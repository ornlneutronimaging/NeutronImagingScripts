"""Console entry points for NeutronImaging.

The modules here own the CLI logic; the files under ``scripts/`` are thin
wrappers kept for callers that invoke the scripts by path (e.g. SNS/HFIR
autoreduction).
"""
